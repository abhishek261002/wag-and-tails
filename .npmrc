# This workspace intentionally runs three incompatible React/React Native
# generations side by side (customer-mobile on RN 0.86/React 19,
# partner-mobile on RN 0.74/React 18.2, the web apps on React 18.3), so a
# strict peer-dependency check across the tree is never fully satisfiable.
legacy-peer-deps=true
